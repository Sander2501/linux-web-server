services:
  web:
    image: nginx:latest
    container_name: linux-web-server
    restart: unless-stopped
    ports:
      - "8082:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

